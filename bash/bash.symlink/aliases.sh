# Colorize ls
alias ls="ls -G"

# Quick access to ls -la
alias lsa="ls -la"

# Cleaning up
alias c="clear"

# Git
alias gs="git status"
alias ga="git add"
alias gc="git commit"
alias gp="git push origin"
alias gsu="git submodule update --init --recursive"
